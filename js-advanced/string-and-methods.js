// problem solved------------------------------>346
// Find index of 'g' in a string
const city = "Chattogram";
const indexG = city.indexOf("g");
console.log(indexG);

// problem solved------------------------------>347
// Check if string includes 'y'
const division = "Sylhet";
const hasY = division.includes("y");
console.log(hasY);

// problem solved------------------------------>348
// Get the last character of a string
const name = "Rifat";
const lastChar = name[name.length - 1];
console.log(lastChar);

// problem solved------------------------------>349
// Find the starting index of substring 'rip'
const language = "javascript";
const indexRip = language.indexOf("rip");
console.log(indexRip);

// problem solved------------------------------>350
// Check string length and if it contains 'mutale'
const text = "Immutable";
console.log(text.length);
console.log(text.includes("mutale"));
