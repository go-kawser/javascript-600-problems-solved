// problem solved------------------------------>548
// What is with?
// with statement extends the scope chain (deprecated)
console.log(
  "'with' extends scope chain but is deprecated due to performance issues and ambiguity"
);

// problem solved------------------------------>549
// What is eval() and why not to use it
// eval() executes JavaScript code from string
console.log(
  "eval() is dangerous: security risks, performance issues, hard to debug, deprecated in strict mode"
);
