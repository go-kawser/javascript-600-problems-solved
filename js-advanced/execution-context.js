// problem solved------------------------------>537
// What is Execution Context?
// Execution Context: Environment where JavaScript code is executed
console.log(
  "Execution Context: Contains variable environment, scope chain, and 'this' binding"
);
console.log(
  "Works in phases: 1. Creation (hoisting), 2. Execution (code runs)"
);

// problem solved------------------------------>538
// Global Context purpose
// Global Context: Base execution context
console.log(
  "Global Context: Provides global object (window in browsers), sets up global scope"
);
console.log(
  "Needed as starting point for code execution, contains all global variables/functions"
);
