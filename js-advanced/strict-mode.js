// problem solved------------------------------>554
// Why use strict mode?
// Strict mode enables stricter parsing and error handling
console.log(
  "Strict mode: Prevents errors, secures code, disables bad features, improves performance"
);
